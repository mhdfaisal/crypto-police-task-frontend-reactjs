import React from "react";

const HeroSubHeading = props => {
  return (
    <h3 {...props}>
      You need to create frontend and backend part of this webpage
    </h3>
  );
};

export default HeroSubHeading;
